[package]
name = "rustyecho-core"
version = "0.1.0"
edition.workspace = true

[dependencies]
thiserror.workspace = true
async-trait = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
